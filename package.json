{
  "name": "bot-factory-site",
  "version": "1.0.0",
  "private": true,
  "description": "Bot Factory landing — статичный сайт студии (Eleventy SSG).",
  "scripts": {
    "dev": "eleventy --serve --port=8765",
    "build": "eleventy",
    "clean": "node -e \"require('node:fs').rmSync('_site',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
